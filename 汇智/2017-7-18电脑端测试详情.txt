一、未登录时（共同问题）
    1、首页警告：The prop `children` is marked as required in `Drawer`, but its value is `undefined`.
    2、警告：Warning: [react-router] `props.history` and `context.history` are deprecated. Please use `context.router`.
    3、点击714那张图片会跳转到交易明细页面
    4、有个 http://222.73.203.71:8080/undefined 

二、admin账号登录
    1、余额查询，卡号不存在，目测是卡的问题，不是我们的；
    2、报修，显示提交成功，但是我的服务中没有显示
    3、我的小智页面，侧滑无限向下延伸；
    4、绑卡界面>汇智卡使用帮助，只有第一个有内容，其它三个没有（应该正常）

三、ptyh账号登录
    1、首页轮播图加载不出来（会导致报错和警告）
    2、充值时卡号不存在
    3、交易明细，返回成功但是没数据

四、新注册的账号，qwer1234/qwer1234
    1、登录重定向有bug，重定向到我的小智时会出问题；（已解决）
    2、未绑卡界面>充值到红包管理没有添加连接（是否需要添加）
    3、未绑卡时，进不了我的小智界面（是否需要修改）
    4、进入个人资料时报错，错误原因：给居住地、国籍和学历赋初始值时报错，因为新用户这些资料为空

五、其它小问题
    1、充值页面：用户名不太明显，乍一看没看到
    2、退出登录后，首页轮播图也没有加载出来
    3、记录最后一个状态，首页、活动中心、我的服务是否都要添加
    4、弹窗不美观，弹窗的位置：注销登录、解绑、绑卡、挂失、和表单验证
    5、忘记密码的功能没做
    6、支付记录接口被废弃，是否把支付记录的按钮去掉
    7、二级联动有时会报key值问题，原因不明
    8、通知管理右上角的红色角标
    9、再次运行时，没有自动登录上去

六、未测试功能
    1、注册
    2、修改手机号
    3、绑定/解绑、挂失、解挂